---
import Icon from '../components/icon.astro';
const title = 'Designing Stories';
---

<section class="hero">
    <div class="hero-dslogo">
        <Icon icon="logo" />
    </div>
    <header>
        <h1 class="visually-hidden">{title}</h1>
        <p class="hero-intro bounceText">A space cultivated<br /> 
            by <a href="/about/" id="link-about" class="link-about">Jesus Acosta</a></p>
    </header>
</section>
<div class='js-lottie lottie-container' id='lottie-container'></div>

    <script>
		import lottie from "lottie-web";
        fetch('/images/talltree.json')
            .then(res => res.json())
            .then(tree => {
                lottie.loadAnimation({
                    container: document.getElementById('lottie-container'),
                    renderer: "svg",
                    loop: false,
                    autoplay: true,
                    animationData: tree,
                });
            });
    </script>
